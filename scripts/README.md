# Repository scripts

The maintained browser suite and its lifecycle runner live under `e2e/`.

```bash
make verify-e2e
```

The coverage migration and rationale for retired root scripts are recorded in [`docs/testing/e2e-coverage-matrix.md`](../docs/testing/e2e-coverage-matrix.md).
